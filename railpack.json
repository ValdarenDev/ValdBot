{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "mkdir -p dist",
        "for item in *; do [ \"$item\" != \"dist\" ] && cp -R \"$item\" dist/; done",
        "cd dist && npm install"
      ]
    }
  },
  "deploy": {
    "inputs": [{ "step": "build" }],
    "startCommand": "npm start",
    "dir": "dist"
  }
}